# Technologies

```
1. HTML
2. Tailwind
3. Javascript
4. React
```

- Flow:

- 2 Screen Application
  - Screen 1
  - Home screen where user can create, edit, delete folder & playground(files) inside a folder
  - Screen 2
  - Code Editor with sytanx highlighting
    - Features
      - User can import code, input data system files.
      - User can export code, output data to system.
      - Able to switch between different themes of the editor.
      - Able to change the language
      - Full Screen Mode
